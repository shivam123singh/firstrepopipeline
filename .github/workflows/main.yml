name: firstrepoPipe
on:
  push:
    branches:
      - node.js

 

jobs:
  build-and-push:
    runs-on: ubuntu-latest

 

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

 

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ 'shivamsinghhhhhh' }}
          password: ${{ 'dckr_pat_lw8mm1jnblV8iLUrn9J5i6De-lI' }}

 

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: shivamsinghhhhhh/getting-started:latest
